import 'package:flutter/material.dart';

/// Cores de sentido (sucesso/erro) usadas em toda a app para receitas,
/// despesas e progresso — separadas da cor de marca (`PulsoColors.primary`).
class SemanticColors {
  SemanticColors._();

  static const receita = Color(0xFF388E3C);
  static const despesa = Color(0xFFE5484D);
}
